--- a/packages/coding-agent/src/modes/daemon/daemon-session-summarizer.ts
+++ b/packages/coding-agent/src/modes/daemon/daemon-session-summarizer.ts
@@ -207,7 +207,7 @@
 
 // Recap prefix for a turn that errored; the transcript's own error text follows
 // it so the persisted verdict reports the real last event, never invented work.
-const ERROR_RECAP_PREFIX = "Model request failed";
+export const ERROR_RECAP_PREFIX = "Model request failed";
 // Generous; the agents view truncates recaps further for display.
 const ERROR_RECAP_MAX_CHARS = 160;
 
--- a/packages/coding-agent/src/modes/interactive/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
@@ -176,6 +176,7 @@
 } from "../agent-connection/index.js";
 import { AgentConnectionPromptAdmissionError } from "../agent-connection/index.js";
 import type { SessionSummary } from "../daemon/daemon-session-list.js";
+import { ERROR_RECAP_PREFIX } from "../daemon/daemon-session-summarizer.js";
 import { getModelArgumentCompletions } from "../model-autocomplete.js";
 import {
 	checkForPackageUpdates,
@@ -1005,6 +1006,12 @@
 	private workingTimer: NodeJS.Timeout | undefined = undefined;
 	private pulseTimer: NodeJS.Timeout | undefined = undefined;
 	private pulseFrame = 0;
+	private lastTerminalTitle: string | undefined;
+	private turnErrored = false;
+	// ⬒ ⬓ ■ share one width in Adwaita Sans; upstream ◈ falls back to a font of another width and shifts the title text.
+	private static readonly TITLE_WORKING_FRAMES = ["⬒", "⬒", "⬓", "⬓"] as const;
+	// Headline fallback before the daemon summarizer writes a recap.
+	private firstUserMessage: string | undefined;
 	private readonly activityTracker = new AgentActivityTracker();
 	// activityTracker token count already folded into the context snapshot; only output beyond
 	// this counts as live in-flight (keeps auto-retries from re-adding a failed attempt).
@@ -1631,14 +1638,40 @@
 		})();
 	}
 
+	// "<status glyph> <headline>": Zed terminal threads split the glyph off as a status marker.
 	private updateTerminalTitle(): void {
-		const cwdBasename = path.basename(this.getCurrentCwd());
-		const sessionName = this.getCurrentSessionName();
-		if (sessionName) {
-			this.ui.terminal.setTitle(`${APP_TITLE} - ${sessionName} - ${cwdBasename}`);
-		} else {
-			this.ui.terminal.setTitle(`${APP_TITLE} - ${cwdBasename}`);
-		}
+		const recap = this.connectionState?.recap ?? this.sessionRecap;
+		// A failure recap sticks until the summarizer next succeeds, so keep it only while that failed turn is still the last one.
+		const staleFailure = recap?.startsWith(ERROR_RECAP_PREFIX) && (this.isAgentStreaming() || !this.turnErrored);
+		const headline = [this.getCurrentSessionName(), staleFailure ? undefined : recap, this.firstUserMessage]
+			.map((candidate) => candidate?.replace(/\s+/g, " ").trim())
+			.find((candidate) => candidate);
+		const text = headline
+			? headline.length > 80
+				? `${headline.slice(0, 79)}…`
+				: headline
+			: `${APP_TITLE} - ${path.basename(this.getCurrentCwd())}`;
+		const frames = InteractiveMode.TITLE_WORKING_FRAMES;
+		const glyph = this.isAgentStreaming() ? frames[this.pulseFrame % frames.length] : "■";
+		const title = `${glyph} ${text}`;
+		if (title === this.lastTerminalTitle) return;
+		this.lastTerminalTitle = title;
+		this.ui.terminal.setTitle(title);
+	}
+
+	private setFirstUserMessage(messages: readonly AgentMessage[]): void {
+		this.firstUserMessage = messages
+			.filter((message) => message.role === "user")
+			.map((message) =>
+				typeof message.content === "string"
+					? message.content
+					: message.content
+							.filter((block) => block.type === "text")
+							.map((block) => block.text)
+							.join(" "),
+			)
+			.find((content) => content.trim());
+		this.updateTerminalTitle();
 	}
 
 	async run(): Promise<InteractiveModeRunResult> {
@@ -3539,6 +3572,7 @@
 
 	// Recover the in-flight run's start from a restored transcript so the elapsed timer survives re-attach.
 	private restoreTurnStartFromMessages(messages: readonly AgentMessage[]): void {
+		this.setFirstUserMessage(messages);
 		this.turnStartedAt = undefined;
 		if (!this.isAgentStreaming()) return;
 		for (let i = messages.length - 1; i >= 0; i--) {
@@ -3576,12 +3610,14 @@
 		this.statusContainer.clear();
 	}
 
+	// Every connection state patch (streaming, name, recap) lands here, so it also refreshes the title.
 	private updateWorkingPulse(): void {
 		const active =
 			this.isAgentStreaming() ||
 			this.chatContainer.children.some(
 				(component) => component instanceof ToolExecutionComponent && component.hasRunningBackgroundShell(),
 			);
+		this.updateTerminalTitle();
 		if (!active) {
 			this.stopWorkingPulse();
 			return;
@@ -3596,6 +3632,7 @@
 		this.updateWorkingPulse();
 		this.pulseFrame += 1;
 		setWorkingPulseFrame(this.pulseFrame);
+		this.updateTerminalTitle();
 		this.ui.requestRender();
 	}
 
@@ -5892,8 +5929,13 @@
 				break;
 
 			case "message_end":
-				if (event.message.role === "user") break;
+				if (event.message.role === "user") {
+					this.turnErrored = false;
+					if (!this.firstUserMessage) this.setFirstUserMessage([event.message]);
+					break;
+				}
 				if (event.message.role === "assistant") {
+					this.turnErrored = event.message.stopReason === "error";
 					this.streamingMessage = event.message;
 					let errorMessage: string | undefined;
 					if (this.streamingMessage.stopReason === "aborted") {
