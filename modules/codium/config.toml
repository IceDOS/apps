[icedos.applications.codium.users.username]
autoSave = "off" # Options: 'off', 'afterDelay', 'onFocusChange', 'onWindowChange'
colorTheme = ""
fontSize = 0 # 0 = use stylix size when stylix is on, else fallback 14
formatOnPaste = true
formatOnSave = true
zoomLevel = 1.0
